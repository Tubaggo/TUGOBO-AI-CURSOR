import { ModulePlaceholder } from "../_components/module-placeholder";

export default function AppSettingsPlaceholderPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Billing, integrations, team access, and per-organization controls will replace this placeholder once authz is fully scoped."
    />
  );
}
