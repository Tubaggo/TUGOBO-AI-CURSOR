import { ModulePlaceholder } from "../_components/module-placeholder";

export default function PropertyPlaceholderPage() {
  return (
    <ModulePlaceholder
      title="Property"
      description="Rooms, rate plans, and on-property configuration will live here as we replace demo-only property mocks."
    />
  );
}
